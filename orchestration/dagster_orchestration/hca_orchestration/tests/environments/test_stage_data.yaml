resources:
  load_tag:
    config:
      load_tag_prefix: fake_load_tag
      append_timestamp: true
  staging_bucket_config:
    config:
      staging_bucket_name: example_staging_bucket_name
      staging_prefix_name: example_staging_prefix_name
solids:
  pre_process_metadata:
    config:
      input_prefix: example_input_prefix
  create_staging_dataset:
    config:
      staging_bq_project: fake_bq_project
      staging_dataset_prefix: testing_dataset_prefix
      staging_table_expiration_ms: 1
